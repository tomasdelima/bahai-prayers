'use strict'

import React, {Component} from 'react'
import {
  View,
  ListView,
  Text,
  TouchableHighlight,
} from 'react-native'

const s = require('./styles')

module.exports = React.createClass({
  render () {
    return <View style={[{}]}>
      <Text>Theme</Text>
    </View>
  },
})

